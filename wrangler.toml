name = "cymatic-resonance"
pages_build_output_dir = "dist"

# Backend configuration is intentionally NOT stored here.
# Set VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY as build-time environment
# variables in the Cloudflare Pages project settings (per environment).
